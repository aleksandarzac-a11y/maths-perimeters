# Deployment - Perimeter Explorer

## Slug
`maths-perimeters`

## Repository
`aleksandarzac-a11y/maths-perimeters`

## Vercel Project Name
`maths-perimeters`

## Dev Server Port
`4007`

## Commands
```bash
npm install
npm run dev -- --port 4007
npm run build
vercel --prod
```

## Environment Variables
Use platform defaults. Store secrets in `.env.local`, GitHub Actions secrets, or Vercel environment variables. Do not commit secrets.

## PWA Manifest
Name: `Perimeter Explorer`
Short name: `Perimeters`
Subject: `Mathematics`
Skills: `perimeter`, `metric measurement`, `geometry`, `missing side lengths`

## Embed
The production Vercel URL must be iframe-compatible for Interactive Maths.
